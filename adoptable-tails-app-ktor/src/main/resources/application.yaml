ktor:
  application:
    modules:
      - ru.otus.otuskotlin.adoptabletails.app.ktor.ApplicationKt.module
  deployment:
    port: 7101
  watch:
    - classes
    - resources
adoptable-tails:
  repository:
    test: "cassandra"
    prod: "cassandra"
    cassandra:
      host: cassandra
      port: 9042
      keyspace: test_keyspace
      username: cassandra
      password: cassandra

jwt:
  issuer: "adoptable-tails-exchange"
  audience: "ad-user"
  realm: "adoptable-tails-exchange-realm"
  clientId: "adoptable-tails-exchange-ad"
  certUrl: "http://localhost:8081/auth/realms/crypto-exchange/protocol/openid-connect/certs" # TODO()